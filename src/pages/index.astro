<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hackerspace Castellón</title>
    
    <!-- Meta refresh INMEDIATO -->
    <meta http-equiv="refresh" content="0; URL=/es/">
    
    <!-- JavaScript inmediato SIN delay -->
    <script>
        // Redirección inmediata sin esperar a window.onload
        (function() {
            const supportedLocales = ['es', 'ca', 'en'];
            const userLang = (navigator.language || '').split('-')[0];
            const targetLang = supportedLocales.includes(userLang) ? userLang : 'es';
            window.location.replace(`/${targetLang}/`);
        })();
    </script>
    
    <!-- Redirección de emergencia -->
    <noscript>
        <meta http-equiv="refresh" content="0; URL=/es/">
    </noscript>
</head>
<body style="background:#000;color:#00ff00;font-family:monospace;text-align:center;padding-top:45vh;">
    <div>HackCS → Redirigiendo...</div>
</body>
</html>