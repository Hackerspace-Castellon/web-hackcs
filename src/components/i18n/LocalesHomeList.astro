---
import { LOCALES } from "@/i18n";
---

<ul>
  {
    Object.entries(LOCALES).map(([lang, { label }]) => (
      <li>
        <a
          href={`/${lang}/`}
          onclick={`localStorage.selectedLang = '${lang}'; location.href = '/${lang}/';`}
          dir={LOCALES[lang].dir || null}
        >
          {label}
        </a>
      </li>
    ))
  }
</ul>
